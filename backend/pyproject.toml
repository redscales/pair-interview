[project]
name = "recipe-backend"
version = "0.1.0"
description = "Recipe library backend (interview material)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy>=2.0",
    "pydantic>=2.9",
]

[tool.uv]
package = false
